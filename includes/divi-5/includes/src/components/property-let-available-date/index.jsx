import metadata from './module.json';
import { registerPropertyMetaModule } from '../property-meta/registerPropertyMetaModule';

registerPropertyMetaModule(metadata, {
  moduleClassName: 'propertyhive_divi5_property_let_available_date',
  outputClassName: 'propertyhive-divi5-property-let-available-date',
  textAttrName: 'letAvailableDateText',
  sampleValue: 'Available now',
  defaultAfter: '',
  hasIcon: true,
});
